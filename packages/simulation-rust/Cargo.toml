[package]
name = "simulation-rust"
version = "0.1.0"
edition = "2021"
description = "EV Charging Station Simulation (Rust implementation)"

[dependencies]
rand = "0.8"
clap = { version = "4.4", features = ["derive"] }
